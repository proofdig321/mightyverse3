{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "mcp-server/Dockerfile"
  },
  "deploy": {
    "startCommand": "python mcp_coordinator.py",
    "healthcheckPath": "/api/mcp/health",
    "healthcheckTimeout": 300
  }
}